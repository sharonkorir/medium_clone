import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    bg: {
      grey: "#f9f9f9",
    },
    border: {
      grey: "#f2f2f2",
    },

    text: {
      grey: "#6b6b6b",
    },
  },
});

export default theme;
